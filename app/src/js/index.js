// import 'babel-polyfill';
import './main.js';

// index.js is the js entry point. It should only import polyfill (if any) and THEN (and only THEN) import actual js files.

// DO NOT write any code except for imports in here. Will very likely cause issues (if you use modern js).